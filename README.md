# autorize (rewrite in progress)

This repository is being migrated from a legacy Jython/Python Burp extension to a Java JAR Burp extension.

* Legacy code: `legacy/`
* New Java rewrite (scaffold): `new/`

## New build notes (Montoya)

The `new/` rewrite uses the Montoya API (Maven Central dependency in `new/build.gradle`).

